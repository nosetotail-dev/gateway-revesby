{
  "name": "sass-base-html",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded shared/scss:shared/css",
    "sass-prod": "sass --no-source-map --style=compressed shared/scss:shared/css"
  },
  "author": "DF",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.64.1"
  }
}
